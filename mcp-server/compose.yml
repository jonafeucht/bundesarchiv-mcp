services:
  bundesarchiv-mcp:
    image: ghcr.io/jonafeucht/bundesarchiv-mcp:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - pdf_data:/app/pdfs
      - faiss_data:/app/faiss_index
    restart: unless-stopped

volumes:
  pdf_data:
  faiss_data:
